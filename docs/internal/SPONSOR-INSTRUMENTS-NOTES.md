# Sponsor instruments package (2026-09-15)

Branch `ui/sponsor-instruments` extends PR #206 with:

- ToolsHub mini instruments (gauge / spark / depth / network) per tool card
- DepthBarChart across Protocol Engine, Healthspan, Biomarker dashboard, Stack Simulator, Biomarker Impact, SynergyScorePanel
- Homepage `HomeInstrumentStrip` + dashboard `DashboardInstrumentPanel`
- Premium `ToolEmptyState` + Phase 2 density (library browse disclosure, HomeDescent Acts 1–3 collapse, mobile ContextBar, stacks equal tabs, labs tile balance)

See `docs/COPILOT-UI-UPGRADE.md` Phase checklist (Phase 1/2 marked done).


## HomeDescent Phase 2 collapse

`HomeSystemOvertureGate` hides `#molecule` / `#system` / `#goal` by default and exposes Explore the system — keeps deferred stages in DOM for integrity pins.
