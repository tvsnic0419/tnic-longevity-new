'use client';

// Clean state - original dynamic rendering restored
export default function CompoundPage({ params }: { params: { id: string } }) {
  return null; // Original dynamic component should render here
}
